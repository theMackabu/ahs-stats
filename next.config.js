module.exports = {};

/* 
  if (sheetData[0]?.data.filter((x) => x.Date === formatDateMonth && x.Assignment !== undefined)[0]) {
    var currentHomeWork = sheetData[0]?.data.filter((x) => x.Date === formatDateMonth)[0].Assignment;
    var currentHomeWorkNumber = sheetData[0]?.data.filter((x) => x.Date === formatDateMonth)[0]["HW #"];
    } else if (
    sheetData[0]?.data.filter((x) => x.Date === `${new Date().getMonth() + 1}/${new Date().getDate() - 2}` && x.Assignment !== undefined)[0]
    ) {
    var currentHomeWork = sheetData[0]?.data.filter((x) => x.Date === `${new Date().getMonth() + 1}/${new Date().getDate() - 2}`)[0].Assignment;
    var currentHomeWorkNumber = sheetData[0]?.data.filter((x) => x.Date === `${new Date().getMonth() + 1}/${new Date().getDate() - 2}`)[0]["HW #"];
    } else if ((sheetData[0]?.data.filter((x) => x.Date === `${new Date().getMonth() + 1}/${new Date().getDate() - 1}`)[0]?.Assignment) === undefined) {
    var currentHomeWork = sheetData[0]?.data[sheetData[0]?.data.length-1].Assignment
    var currentHomeWorkNumber =  sheetData[0]?.data[sheetData[0]?.data.length-1]["HW #"];      
    } else {
    var currentHomeWork = sheetData[0]?.data.filter((x) => x.Date === `${new Date().getMonth() + 1}/${new Date().getDate() - 1}`)[0].Assignment;
    var currentHomeWorkNumber = sheetData[0]?.data.filter((x) => x.Date === `${new Date().getMonth() + 1}/${new Date().getDate() - 1}`)[0]["HW #"];
    }
*/
